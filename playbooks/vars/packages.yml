---
packages_master:
  # Terminal multiplexer
  - modules:
      pacman: zellij
  # Source code repository manage
  - modules:
      pacman: ghq
  - modules:
      pacman: github-cli
  # tig
  - modules:
      pacman: tig
      homebrew: tig
  - modules:
      pacman: lazygit
  # NeoVim: As bare Vim (vim is used many plugin as small IDE)
  - modules:
      pacman: neovim
      homebrew: neovim
  # Django(for django-admin)
  - modules:
      pip: django
  # nvm (Node executable manager)
  - modules:
      homebrew: nvm
  # httpie
  - modules:
      pip: httpie
  # cookiecutter
  - modules:
      pip: cookiecutter
  # poetry
  - modules:
      pip: poetry
  # Formatting python sources
  - modules:
      pip: black
  # IaC
  - modules:
      pacman: terraform
